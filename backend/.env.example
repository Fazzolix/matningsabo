# Flask Secret Key (change this to a random string)
SECRET_KEY="a_very_secret_key_that_you_should_change"

# Azure AD App Registration Details
AZURE_CLIENT_ID="your_azure_client_id"
AZURE_TENANT_ID="your_azure_tenant_id"

# Frontend URL for CORS
# For local development, this is typically http://localhost:3000
# In production, this should be the URL of your Cloud Run service
FRONTEND_URL="http://localhost:3000"

# Superadmin bootstrap email (must match Azure user's email)
SUPERADMIN_EMAIL="super.admin@example.com"

# Google Cloud Credentials
# In Cloud Run, you don't need to set this if the service account has the correct permissions.
# For local development, uncomment and set the path to your service account JSON file.
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/service-account-file.json"  # legacy, ej i bruk på Azure

# Azure Cosmos DB (NoSQL/SQL-API)
COSMOS_ENDPOINT="https://<ditt-cosmos>.documents.azure.com:443/"
COSMOS_KEY="<primar-nyckel>"
COSMOS_DATABASE="traffpunkt"
COSMOS_CONTAINER_ATTENDANCE="attendance_records"
COSMOS_CONTAINER_ACTIVITIES="activities"
COSMOS_CONTAINER_TRAFFPUNKTER="traffpunkter"
COSMOS_CONTAINER_USERS="Users_traffpunkt"
COSMOS_CONTAINER_ADMIN_AUDIT="Admin_audit_traffpunkt"
COSMOS_CONTAINER_ATTENDANCE_AUDIT="Attendance_audit_traffpunkt"
